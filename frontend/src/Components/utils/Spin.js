import React from 'react'
import { HashLoader } from'react-spinners';

export default function Spin () {
    return (
        <HashLoader
            color="#73f2bb"
            loading
        />
    )
}
